import { render, screen } from '@testing-library/react'
import { navigateTo } from './utils'

describe('Router', () => {

  it('should', () => {

    navigateTo('/login');

    // screen.getByRole('heading');
  })
})